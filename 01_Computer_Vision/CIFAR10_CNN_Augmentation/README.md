# CIFAR10 CNN Augmentation

## Project Overview
I built a CNN for image classification that utilizes data augmentation layers like random flips and zooms to improve model generalization and prevent overfitting on small datasets